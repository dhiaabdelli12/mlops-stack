[tool.poetry]
package-mode = false 

[tool.poetry.dependencies]
python = ">=3.12"
pandas = "^2.2.3"
dotenv = "^0.9.9"
pytest = "^8.3.5"


[tool.poetry.group.data_ingestion.dependencies]
ucimlrepo = "^0.0.7"
sqlalchemy = "^2.0.40"
psycopg2-binary = "^2.9.10"


[tool.poetry.group.data_exploration.dependencies]
plotly = "^6.0.1"
psycopg2-binary = "^2.9.10"
fastparquet = "^2024.11.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
